 <%= form_tag charges_path do %>
    <article>
        <label class="amount">
          <h4>200ul Annual Subscription: $195</h4>
        </label>
    </article>

      <script src="https://checkout.stripe.com/v2/checkout.js"
              class="stripe-button"
              data-key="<%= Rails.configuration.stripe[:publishable_key] %>"
              data-description="200 ul Annual Subscription"
              data-amount="19500">
      </script>
      <label class="amount">
          <h4>No credit card? <%= link_to "RECEIVE A QUOTE", two_hundred_ml_path(anchor: "quote_report") %></h4>
      </label>

<% end %>
